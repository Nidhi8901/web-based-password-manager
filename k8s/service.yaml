apiVersion: v1
kind: Service
metadata:
  name: password-manager-service
spec:
  type: NodePort
  selector:
    app: password-manager
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
